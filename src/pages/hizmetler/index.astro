---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { services } from '../../data/services';
---

<BaseLayout
  title="Hizmetler"
  description="İscotera'nın sunduğu yazılım hizmetleri: web uygulama, mobil uygulama, özel yazılım ve danışmanlık."
  lang="tr"
  alternate="/en/services/"
  back={{ href: '/', label: 'Ana Sayfa' }}
>
  <section class="mx-auto max-w-5xl px-4 py-16">
    <h1 class="text-3xl font-bold text-ink-strong">Hizmetlerimiz</h1>
    <p class="mt-3 max-w-2xl text-ink-muted">
      İhtiyacınıza göre uçtan uca yazılım hizmetleri sunuyoruz.
    </p>
    <div class="mt-8 grid gap-6 sm:grid-cols-2">
      {
        services.map((service) => (
          <a
            href={`/hizmetler/${service.tr.slug}/`}
            class="rounded-lg border border-line p-6 transition hover:border-brand/40 hover:shadow-md"
          >
            <h2 class="font-semibold text-ink-strong">{service.tr.title}</h2>
            <p class="mt-2 text-sm text-ink-muted">{service.tr.summary}</p>
          </a>
        ))
      }
    </div>
  </section>
</BaseLayout>
