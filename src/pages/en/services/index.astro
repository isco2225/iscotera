---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import { services } from '../../../data/services';
---

<BaseLayout
  title="Services"
  description="Software services offered by İscotera: web applications, mobile apps, custom software and consulting."
  lang="en"
  alternate="/hizmetler/"
  back={{ href: '/en/', label: 'Home' }}
>
  <section class="mx-auto max-w-5xl px-4 py-16">
    <h1 class="text-3xl font-bold text-neutral-900">Our Services</h1>
    <p class="mt-3 max-w-2xl text-neutral-600">
      End-to-end software services tailored to your needs.
    </p>
    <div class="mt-8 grid gap-6 sm:grid-cols-2">
      {
        services.map((service) => (
          <a
            href={`/en/services/${service.en.slug}/`}
            class="rounded-lg border border-neutral-200 p-6 transition hover:border-brand/40 hover:shadow-md"
          >
            <h2 class="font-semibold text-neutral-900">{service.en.title}</h2>
            <p class="mt-2 text-sm text-neutral-600">{service.en.summary}</p>
          </a>
        ))
      }
    </div>
  </section>
</BaseLayout>
