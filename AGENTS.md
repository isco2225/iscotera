# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

Corporate marketing site for İscotera (a software company), served at https://iscotera.com. Fully static Astro 7 + Tailwind CSS 4 build. The primary goals are SEO/brand visibility, so preserving the SEO plumbing (canonical, hreflang, sitemap, JSON-LD) on every page matters more than usual.

Repository: https://github.com/isco2225/iscotera. Site content is written in Turkish and English; **commit messages must be in English**, and code comments inside `src/` are written in Turkish to match the existing files.

`CLAUDE.md` is a symlink to this file (`AGENTS.md`), so both agent tools read the same guidance — edit `AGENTS.md`, never replace the symlink with a copy.

## Commands

Requires Node >= 22.12.0 (`engines` in package.json).

- `npm run dev` — dev server at localhost:4321. When starting it from an agent, use background mode: `astro dev --background` (manage with `astro dev stop` / `status` / `logs`). If dev throws `Cannot read properties of undefined` from a collection field that the build handles fine (seen 2026-09-01 as `reading 'appStore'` on every product page), the dev content cache `.astro/data-store.json` predates a schema change — the build keeps its own copy under `node_modules/.astro/`, so the two disagree. `astro dev --force` did not refresh it; stop the server, delete that one file and start again.
- `npm run build` — production build to `dist/` (about a second). Use this to verify changes; there are no tests or linter. The build fails on frontmatter that breaks the `content.config.ts` schemas and on anything that throws while prerendering, but it does not type-check, and neither `typescript` nor `@astrojs/check` is installed. Don't run `astro check`: it blocks on an interactive install prompt. A pure type error (a wrong prop, a string missing from one `ui.ts` table) therefore shows up only in the editor (the Astro VS Code extension is the only recommended one).
- `npm run preview` — serve the production build locally. From an agent, use `astro preview --background`, which takes the same `stop` / `status` / `logs` subcommands. Both servers default to port 4321, so pass `--port` when dev is already running.

Draft content (`draft: true` in frontmatter) renders in dev but is excluded from production builds — so dev and build page counts intentionally differ. The mechanism is not an Astro setting but a filter repeated in every `getCollection` call: `({ data }) => import.meta.env.DEV || !data.draft`. Any new listing, route or lookup over `blog` / `projects` must copy that predicate, or drafts leak into production.

## Architecture

### Bilingual routing (TR default, EN secondary)

- Turkish pages live at the root of `src/pages/` with Turkish slugs (`/hakkimizda/`, `/hizmetler/...`); English pages live under `src/pages/en/` with English slugs (`/en/about/`, `/en/services/...`). Astro's i18n config (`astro.config.mjs`) uses `defaultLocale: 'tr'` with no prefix for Turkish.
- Every page renders through `src/layouts/BaseLayout.astro`, which owns all SEO output: title suffixing, meta description, canonical URL, hreflang link tags, Open Graph tags, and the site-wide JSON-LD `@graph` (see the structured-data bullet below). Pages pass `lang` and an `alternate` prop (the same page's path in the other language); `alternate` drives both the hreflang tags and the header's language switcher. **When adding a page, add both language versions and wire `alternate` in both directions.** Blog detail pages are the deliberate exception (posts aren't 1:1 translated), so they omit `alternate`; the language switcher in `Header.astro` then falls back to the other language's home page, so it never dead-ends. Project detail pages pair themselves: if the same slug exists under both `tr/` and `en/`, the route passes `alternate` automatically; a project that exists in only one language simply gets none. `src/pages/404.astro` is the other single-language file: one Turkish-`lang` page with bilingual copy and links to both home pages, no `alternate`, no `back`.
- Static route pairs (TR path ↔ EN path) and the labels that shared components render live in `src/i18n/ui.ts`; per-language page files write their own copy inline (see Static pages below). New nav items or translated labels go into `ui.ts`, not inline in components. Components read them through `useTranslations(lang)` → `t('key')`. The key type is `keyof (typeof ui)[Lang]`, so only keys present in **both** tables type-check. The source spells it `typeof defaultLang`, which resolves to `Lang` only because `defaultLang` is annotated `: Lang`, so keep that annotation. Every new string is therefore added to `tr` and `en` together, and the runtime `?? ui.tr[key]` fallback never fires as long as the editor's type errors are respected. The projects collection is presented as "Ürünler" / "Products" and its routes match: `/urunler/` and `/en/products/`. They were `/projeler/` and `/en/projects/` until 2026-08-29. The collection directory under `src/content/` is still named `projects/`, and so are the `routes.*.projects` / `nav.projects` keys in `ui.ts`: these are internal names and stay English.
- Inner pages (everything but the two home pages) also pass `BaseLayout`'s `back` prop — `{ href, label }` pointing at the parent listing — which renders the back link above the content. Match this when adding a page. `ServicePage` and `ProductPage` take the label from `ui.ts`; the per-language page files write it inline.
- **Structured data is one `@graph` per page plus optional page-specific blocks.** `BaseLayout` emits a single `application/ld+json` script holding `Organization` (every page, with `@id` `#organization`), `WebSite` (the two home pages only, `@id` `#website`) and `BreadcrumbList` — the breadcrumbs are derived from the `back` prop (home → `back.label` → page title, dropping the middle crumb when the parent *is* home), so a wrong `back` is also wrong SEO output, not just a wrong link. The `@id` helpers and `localeTag` live in `src/utils/schema.ts`; a page-level schema that needs the company references `{ '@id': organizationId(Astro.site) }` instead of restating name/logo/contact. Blog detail pages build their `BlogPosting` from `blogPostingSchema()` in that file (one helper so the two language routes can't drift; author and publisher are both the Organization, matching the visible "İscotera Ekibi" byline), and its `image` is the post's cover when one exists (the same `getImage()` output as the visible `<img>`, so the URL always exists) and absent otherwise — pointing at a missing URL is a Search Console error. Page-specific blocks (`FAQPage` on service and product pages, `SoftwareApplication` / `VideoObject` on product pages, `BlogPosting`, `AboutPage`) go through `BaseLayout`'s `head` slot (`<Fragment slot="head">`, never a second `<head>`) as separate scripts; don't merge them into the layout graph.

### Theming — semantic tokens, never literal colors

- **Never write a literal color utility in a page or component** (`bg-white`, `text-neutral-900`, `border-neutral-200`). Use the semantic tokens defined in `src/styles/global.css`: surfaces are `surface` / `surface-muted` / `surface-raised`, borders are `line` / `line-strong`, text runs `ink-strong` → `ink` → `ink-soft` → `ink-muted` → `ink-subtle` → `ink-faint`, and the accents are `brand`, `action` (filled-button background), `action-dark` (its hover) and `action-ink` (link/heading text). Their light values are exactly the Tailwind `neutral-*` tones the site used before (all declared in a single `@theme` block, so `bg-surface` / `text-ink-muted` etc. are ordinary Tailwind utilities), and `:root[data-theme='dark']` redefines them — so **a page that uses tokens gets dark mode for free and never needs a `dark:` variant.** Literals are allowed only on a colour that does not change between modes, where a token would only hide that. That means `text-white` as the label on a filled `bg-action` button, `bg-black/40` for the mobile-menu scrim, and the contents of a product panel (`ProductPanel.astro`), which sits on the product's own colour: its white text, its white pill button with `text-neutral-900`, and its `green-400` live dot.
- `action` and `action-ink` are the same blue in light mode but deliberately different in dark: no single blue is both readable as text on a dark ground and readable under white button text. Don't collapse them back into one.
- The mode lives in `data-theme` on `<html>`. `BaseLayout` writes it from `localStorage` (falling back to `prefers-color-scheme`) in an `is:inline` script that **must stay the first script in `<head>`** — bundling or deferring it brings back a white flash on every navigation in dark mode. The two-state control is `ThemeSwitch.astro`, rendered twice — once in the desktop nav, once in the header bar beside the hamburger under `md:hidden`, so mobile users reach it without opening the menu (the drawer deliberately has no third copy). Its knob position and icon emphasis come from `dark:` classes, not JavaScript, so it is already in the right position on first paint; the script only syncs `aria-checked` and persists the choice. Both instances carry `[data-theme-toggle]`, which is how the script finds them. The inline script defines `window.__setTheme(theme)`, which sets `data-theme`, updates the `<meta name="theme-color">` (the mobile browser-bar colour can't follow `prefers-color-scheme` because the user may pick the opposite of their system setting) and syncs `aria-checked`; `Header.astro`'s toggle calls that same global rather than duplicating the logic, so a new colour value is changed in one place.
- The theme preference is the site's **only** browser storage, and there are no cookies anywhere in the codebase. It stores one key (`theme` = `"dark"` / `"light"`), first-party, never transmitted, no identifier — a UI preference set by the user's own click, which is the category consent rules exempt. So no consent banner is needed for it, and the `try/catch` around both calls is there because private-mode Safari and storage-blocked browsers throw. **Re-open this question when analytics lands** (it's on the README pre-launch list): the exemption reasoning here covers the theme toggle only and does not carry over.
- **Component-scoped `<style>` blocks do work, but they don't land in the CSS bundle.** Astro inlines small stylesheets (`build.inlineStylesheets: 'auto'`) as a `<style>` in the `<head>` of every page that renders the component. To check that a rule shipped, grep the built HTML, not `dist/_astro/*.css`. This was re-tested on 2026-09-23: moved into `Faq.astro`, the Safari marker rule came out as `summary[data-astro-cid-…]::-webkit-details-marker` in the service pages' HTML. That overturns the 2026-08-29 note that such blocks were silently dropped, which was most likely a search of the CSS bundle alone. The rules Tailwind utilities can't express still live in `global.css`: `summary::-webkit-details-marker` for the FAQ accordion in Safari (`Faq.astro`, the one `<details>` accordion shared by `ServicePage` and `ProductPage`), the `.process-fill` scroll-timeline keyframes for the home process section and the `.hero-*` layers.
- **Never combine the `animation` shorthand with `animation-timeline` in one rule.** The CSS minifier folds them into a single shorthand, Chrome rejects that syntax and drops the whole animation (this happened, 2026-08-29). `.process-fill` is written as longhands: the base block starts `animation-play-state: paused` at the `scaleY(0)` frame, and only the `@supports (animation-timeline: view())` block, itself inside `prefers-reduced-motion: no-preference`, attaches the timeline and flips it to `running`. Keep that shape for any future scroll-linked rule.
- A `dark:` variant exists (`@custom-variant` in `global.css`) for the few things tokens can't reach: the icon swap in the theme button and `dark:prose-invert` on the three `prose` containers (both blog detail pages and the product story), since the typography plugin hardcodes its own colors.
- The İ logo is one component, `src/components/Logo.astro` (size via `class`), used in `Header`, `Footer` and the author line on both blog detail pages; it reads its tile fill from `--logo-tile` / `--logo-stroke`: in dark mode the tile lightens slightly and gains a hairline border, otherwise a black tile on a near-black page disappears. `public/favicon.svg` carries the same treatment via an embedded `prefers-color-scheme` query, and `brand/logo-dark.svg` is the dark-context source asset.

### Content model — four sources, four mechanisms

1. **Services** (`src/data/services.ts`): a single typed array holding an `id`, an optional `caseStudy` (a projects-collection slug, no language prefix) and, per language, slug/title/summary plus the detail-page content (`intro` paragraphs, `scope` cards, `approach` items, `faq`). The dynamic routes `src/pages/hizmetler/[slug].astro` and `src/pages/en/services/[slug].astro` both generate from it — adding an entry creates both detail pages and both listing cards automatically.
   - **Both detail routes render one component**, `src/components/ServicePage.astro` (same reasoning as `ProductPage`): the section order hero → scope → approach → case study → FAQ → CTA is the sales argument. The FAQ entries are real authored content, so the page also emits a `FAQPage` JSON-LD block via the layout's `head` slot.
   - `caseStudy` drives the "work we built with this" box on the detail page, and it only renders when that project entry is actually published (draft-aware `getEntry` check), so a draft product never produces a broken link in production.
   - **The services design is the photo showcase the user picked on a design canvas** (2026-08-29, "Yön D"): the listing is full-width alternating strips — `ServiceStrip.astro`, one per service, `surface-muted` band + red number + summary + photo, photo side alternating — and the detail page follows the same language (red index number in the hero, top-ruled scope columns, a `surface-muted` approach band with red step numbers). The photos live in `src/assets/services/<id>.jpg` (Pexels, free commercial license — sources and the no-visible-brands rule are in that folder's README.md) and each service's `imageAlt` lives per-language in `services.ts`; a service id with no matching photo renders as a single text column.
   - **CSS-drawn mockups and self-playing decorative animation are rejected here** (2026-08-29, two rounds before the photo direction settled): the user had hand-drawn CSS mockups and their micro-animations removed root and all. Real photography (or user-supplied media) is the accepted visual style; don't reintroduce drawn mockups or autoplaying motion, and run visual direction changes past the user on a canvas first. Motion tied to the visitor's own action is a different matter — see the home process section below, which the user asked for.
   - **Adding a service photo is two steps, not one**: drop `src/assets/services/<id>.jpg` in place *and* add it to the `images` map in `ServiceStrip.astro` (the map is explicit imports so Astro can optimize them; nothing is keyed by filename at runtime). The no-visible-brands rule in that folder's README applies to every photo under `src/assets/`.
2. **Blog and projects** (`src/content/blog/{tr,en}/`, `src/content/projects/{tr,en}/`): Markdown content collections defined in `src/content.config.ts`. Language is encoded in the entry id prefix (`tr/...` / `en/...`); listing and detail pages filter on it and strip it to form the URL slug. That schema file is the authoring contract — the project frontmatter carries far more than title/description (`logo`, `cover`, `features`, `results` capped at 3, `stores`, `rating`, `testimonials`, `faq`, `app`, `social`, `links`, `screenshotsIllustrative`, `order`), so read it before hand-writing an entry. The product page renders hero → proof strip → features → story → testimonials → FAQ → related services → closing CTA, and any section whose field is empty is not rendered at all; there is deliberately no separate screenshot gallery (each screen sits beside the feature it illustrates) and no pricing section (`pricing.label` shows as a chip in the hero). Blog entries take an optional `cover` + `coverAlt` (file under `src/content/blog/_images/<slug>/cover.jpg`, Pexels sources and the no-visible-brands rule in that folder's README): the listing and home teasers show it as a full-width 21:9 band above the text via `<Image>` with `blogThumbOptions` (1408 wide, cropped to 21:9 at build time at the detail cover's quality, because the listing shows every post's cover on one page; 16:9 was tried and made the cards too tall) (the user rejected a side-by-side thumbnail layout on 2026-09-10), the detail page shows it wide via one `getImage()` call with `blogCoverOptions` (1536 wide, cropped to 16:9 at build time at quality 70, since it is the page's LCP element) whose URL also feeds the `BlogPosting.image` — the constants live in `src/utils/blog.ts`, so change them there rather than in the calls. The `_images` folders are excluded from both collection globs so their README is not parsed as an entry. There are no sample/template entries in either collection: the blog templates (`ornek-yazi.md` / `sample-post.md`) were removed at the user's request on 2026-09-21, so the schema comments in `content.config.ts`, the README's authoring section and the published posts are the reference for blog frontmatter; don't recreate a sample post. The projects sample (`ornek-proje.md` / `sample-project.md`) went the same way on 2026-09-01; don't recreate a sample product either.
   - **Product teasers are product-coloured panels** (`src/components/ProductPanel.astro`). This is design "H", which the user picked on the products canvas on 2026-09-23. They had said the earlier full-width strips did not look clickable, and they rejected variations that only restyled the button. Each product is one rounded panel, and the whole panel is a single link.
     - **Colour:** the panel sits on the product's own colour, frontmatter `color` (a `#rrggbb` taken from the product's own visuals, neutral `#262626` when unset). The schema rejects a colour whose contrast with white is under 7:1 (`contrastWithWhite` in `src/utils/color.ts`), which leaves room for the 80 % white secondary text.
     - **Text column:** the status chip (its dot green for a live product; the user's call on 2026-09-16, because it reports a state, not identity), the `category · platforms` line, title, tagline, `rating` (Rating's `tone="inverse"`) and `results`, and a white pill button carrying the page's `linkLabel`. The pill is white rather than the site's blue because blue clashes on a coloured ground; the panel itself is the click target.
     - **Visuals:** they bleed off the panel edge. A portrait cover stands as a phone, with the product's first portrait feature image as a second phone behind it from `lg:` up. A landscape cover slides in from the right. Below `md:` the visual drops into a fixed-height band under the text.
     - **Where it's used:** both home pages show the panels under a red "Ürünlerimiz" label with an outlined "Tüm ürünler" button. Both product listings mark their first panel `eager`, because it can be the LCP element. The panel and its visuals lift on hover only under `motion-safe`.
   - **A product's promo video replaces the hero cover without costing the first paint** (frontmatter `video: { src, uploadDate }`, file under `public/media/<slug>/` — Astro doesn't process video). The cover stays as the `poster` so LCP is unchanged; `preload="none"` plus a visibility script means the file streams only once the hero is seen (muted, looped, no controls), a reduced-motion visitor gets a paused video with controls instead, and the page emits a `VideoObject` schema because the video really plays on the page.
   - **The product hero carries a künye (client + sector), never a year or a company-wide count.** `client` and `sector` render as a small two-column `<dl>` under the tagline; `year` stays in frontmatter but is deliberately not printed (the same year under every entry would advertise how young the company is, not the project), and there is no "N+ projects / N+ clients" band anywhere — the user ruled both out on 2026-09-21 after comparing with a larger competitor's site. The only figures shown are the per-product `results`. The related-services block ("services we used on this project", just before the closing CTA) is derived by reverse lookup of `caseStudy` in `services.ts` (no extra frontmatter), so service → case study → service is a closed loop for internal linking.
   - **Both product routes render one component.** `/urunler/[slug]` and `/en/products/[slug]` are thin: they only choose which entry is published where (and pair the two languages for `alternate`); the whole page body lives in `src/components/ProductPage.astro`. The rest of the site is written per language, but the product page deliberately is not — its section order is the sales argument, and two copies would silently drift. Change the page there, and change the strings in `ui.ts`.
   - **The İbadet Rehberim entry is deliberately thin.** It is not a sales page for the app (ibadetrehberim.com already is one); its job is to show a prospective client "we built this, this is how". So it intentionally has no `faq`, no `testimonials`, no `rating` (only the verifiable download count in `results`) and only two of the app's features — the ones that show engineering decisions. Don't fill those sections in to make the page "complete"; it was published on 2026-09-25 without an App Store link (the app is not on the App Store yet, so `stores` carries only Google Play and web, with no `pending` placeholder); add `stores.appStore` when it ships.
   - **Dernek Asistan is a case study that names no client** (`type: case-study`, an internal staff tool). On 2026-09-23 the user asked that the client organisation not be named anywhere in the project, this file included. So the entry has no `client` field and the copy only says that charities use the system; the product page's künye then shows just the sector. There are no `stores`, no `pricing`, and deliberately no `app` block, so the page emits no `SoftwareApplication` schema — reporting an offer and install URL for software nobody can obtain would be fiction. `results` is empty because there are no verified usage figures; the prose describes the benefits qualitatively instead. Its only image is the cover, `_images/dernek-asistan/cover.png`: a composed promo visual the user supplied on 2026-09-21 (laptop dashboard, form and report cards on a desk, sample figures) that replaced all per-feature screenshots — the user asked for this one image and nothing else, so the `features` have no `image` and the old screen captures were deleted; don't re-add feature screenshots here. The figures in it are sample data, so the entry keeps `screenshotsIllustrative: true`, which prints the `product.illustrative` note ("Görüntüdeki veriler temsilidir.") under the cover. If screenshots ever return, capture at DPR 2 — see the image rule below, which this entry's screenshots broke once already.
   - **Case-study copy is written for the client's manager, not for an engineer** (the user's call, 2026-09-04, tightened again 2026-09-21): short, benefit-first, no implementation vocabulary and no screen-level detail either. The prose answers "how does this make the work easier, what got better about tracking and reporting", not "how did we build it" or "which fields does the form have". The Dernek Asistan body went first from a "Nasıl geliştirdik" section with five technical subsections to problem → what changed → today, and then (2026-09-21, after the user read "Çalışan bağışçının adını ve bağış türünü giriyor; fiyatı, tutarı ve sıra numarasını..." and called it needless detail) to problem → today only, with the benefits carried by the `features`: each feature `title` is a benefit headline (time saved, everything on record, easier tracking, consistent data, one-click reports, more donations with the same team), each `text` is at most two sentences, and step-by-step descriptions of the UI ("tabs", "fields", "the form is filled once") are out. Naming the stack, the schema or the enforcement layer belongs in a blog post if anywhere; describing form fields belongs nowhere on the page. Claims stay qualitative because there are no verified figures (see `results`).
   - **Unverified data renders but never enters the schema.** Two frontmatter escape hatches let a page be laid out before the real facts arrive: `placeholderData: ['rating', 'testimonials']` marks a field as made-up (it still shows on the page but is left out of the JSON-LD), and `stores.pending: ['appStore']` marks a store link we don't have yet (the badge prints as a dim, unclickable placeholder — never the official badge art, which is licensed for a link that actually goes to the store). Both exist because reporting an invented `aggregateRating` or review in structured data violates Google's rules and earns a manual action. `warnUnfinishedData` in `src/utils/product.ts` prints a build warning for every entry that still has either list populated, so `npm run build` is the check — **both lists must be empty before an entry flips to `draft: false`.** Note the warning fires from `ProductPage`, i.e. only for entries that actually get a page: a `draft: true` entry is silent under `npm run build` and only warns in dev (when its page is rendered), so run the check right after flipping the flag, not before.
3. **Contact details** (`src/data/contact.ts`): the single source for e-mail, phone (display + E.164), WhatsApp and Instagram. The contact pages, the footer, and the Organization JSON-LD in `BaseLayout` all read from it — change the constant, never the markup. The channel icons (mail, phone, WhatsApp, Instagram) are one component, `ContactIcon.astro`, shared by `ContactCard` and the footer.
   - **The footer is four columns plus a copyright row** (modelled on a competitor footer the user showed on 2026-09-21): brand (Logo + wordmark, the `footer.tagline` string, round Instagram and WhatsApp buttons), pages, services (every entry in `services.ts`, so all four service pages get a site-wide internal link) and contact (e-mail and phone as icon rows in `brand` red, from `contact.ts`; WhatsApp sits with the social buttons, not here). The copyright row's right side holds the other-language link (`alternate` is passed through from `BaseLayout`, falling back to the other home page like the header switcher) — not privacy/KVKK links, because the site has no cookies and no form yet, so those pages don't exist; add them there when the contact form or analytics lands. `sm:` shows two columns with the brand block spanning both, `md:` the full four.
4. **Static pages** (home, about, contact): copy is written inline per language in the page files, so a wording change is made twice (`src/pages/x.astro` and `src/pages/en/x.astro`). All three now carry real copy; `PagePlaceholder` survives only as the empty state of the blog and product listings.
   - **The About page argues from principles, never from figures** (written 2026-09-23 after surveying ~19 studio/agency About pages, TR and international). It has six sections — hero → "Projelerde öngörülebilirlik" (+ photo) → "Çalışma ilkelerimiz" → "Kendi ürünlerimiz" → "Hizmetlerimiz" → CTA — and runs ~500 words, which is where the credible small-studio pages sit. Four rules hold it together, and each of them is a thing the surveyed pages got wrong: **no counts, no founding year and no mention of being young** (the same ban as the product hero; the pages that announce youth read as riskier, not fresher, and one of them listed "uygun fiyat" as a company value); **no Misyon/Vizyon pair**, which is the tell of every weak Turkish page; **every principle is a behaviour a competitor could plausibly contradict** ("Bağımlılık oluşturmayız", "Muhatabınız, projeyi geliştiren ekiptir"), not an adjective like "şeffaflık" or "kalite"; and **proof is a checkable artefact, not an aggregate** — the page points at the published products, the blog and this site itself instead of counting anything.
   - The six principles are deliberately *not* the home page's four process steps: home says what happens in a project and in what order, About says why we work that way. Keep that split — restating the stages here would make one of the two pages redundant. The commitments are also the same promises `services.ts` and the home steps make, so a sentence that will not appear in a real proposal does not belong in the list.
   - The page emits an `AboutPage` block through the `head` slot whose `mainEntity` is `{ '@id': organizationId(...) }` — it points at the layout's Organization rather than restating the company, the same rule as every other page-level schema.
   - **The English page omits the blog link that the Turkish one carries.** `/en/blog/` is deliberately empty (see the asymmetry note below), and sending an English reader to an empty listing as evidence would undercut the exact paragraph it sits in. The rest of the two pages match section for section.
   - Its one photo is `src/assets/about/planning.jpg` (Pexels, source in that folder's README), rendered in the `ServiceStrip` idiom. Inner pages open with a *text* hero — the photo hero belongs to the home page — so the red uppercase label above the `h1` stands in for the red index number the service pages use.
   - **The home hero is layout "K" with photo "R2"** (both picked on the hero design canvas, 2026-09-01): a two-column grid on `md:` — left-aligned heading, copy and buttons on the left, `src/assets/home/meeting-room.jpg` (an empty glass-walled meeting room, Pexels #13323677, source in that folder's README) on the right as a `rounded-2xl` bordered, shadowed 4:3 card capped at 480px and rendered `eager`/`fetchpriority="high"` at width 960 because it is the LCP candidate. Below `md:` the card drops under the text. The user's reference for the photo was an empty modern meeting room with laptops on the table; keep that register (no people, no readable brands) if the photo is ever swapped.
   - **The home hero background is "Yön H"** from the same canvas (picked out of nine directions): a cone-shaped `action`-blue spot from the top, a sparse dot texture fading toward the edges, and a soft-light grain layer. All three are static `aria-hidden` divs inside the `relative overflow-hidden` hero section, styled by `.hero-dots` / `.hero-spot` / `.hero-grain` in `global.css` (mask-image and a data-URI can't be written as Tailwind classes). Colours come from tokens, and the spot's intensity is `--hero-spot` (24% light, 22% dark — light needs more because the blue washes out on white) next to the logo variables. Both home pages carry the same three divs — change them in both. It is decoration without motion, so it sits inside the no-autoplaying-motion rule.
   - **The home process section** ("Çalışma sürecimiz") is a sticky intro column (label, heading, CTA, the team photo from `src/assets/home/team.jpg` — Pexels, source in that folder's README) beside a red-numbered vertical timeline whose guide line fills red as the visitor scrolls. The fill is `.process-fill` in `global.css`, driven by `animation-timeline: view()` — it moves only when the page moves, which is why it is allowed under the no-decorative-motion rule; browsers without scroll timelines and reduced-motion visitors get the plain grey line. The user asked for this explicitly (2026-08-29), and the layout was modelled on an agency site they liked. The `steps` array (title, thesis, text) lives in each home page's frontmatter; the copy makes promises (timeline and cost in writing, maintenance after launch) that must stay true, so don't sharpen them for effect. **No fixed demo or release interval may be promised anywhere.** The site said "a working version every two weeks" in the hero, this step, the About page and a blog post until 2026-09-23, when the user pointed out that the team does not work to that cadence. The development step now says the client witnesses the progress ("İlerlemeye siz de şahit olursunuz", the user's own wording), and the hero says the client can follow it.

### Deliberate decisions (don't "fix" these)

- **Site copy speaks in a corporate, serious register** (the user's call, 2026-09-23: they called the home hero line and the About page "hiç profesyonel değil" and asked for "kurumsal ve ciddi" copy). Turkish copy states things plainly, in the present continuous or a neutral passive ("netleştiriyoruz", "belgelenir"). Use formal verbs ("yazılı olarak belirlemek", "iletişime geçebilirsiniz") and noun-phrase headings rather than questions ("Projelerde öngörülebilirlik", not "Neyi düzeltmeye çalışıyoruz?"). Leave out developer idioms ("tek satır kod yazılmadan"), punchy "X değil, Y" contrasts and aphorisms, cute closing lines ("Geriye konuşulacak sürpriz kalmıyor"), colloquial verbs ("yazılı veririz", "size yıkmayız") and self-praise adjectives such as "dürüst". English copy follows the same register. This rule is about tone only: the substance rules above still hold, and no promise may get stronger in the rewording. Whether blog posts must follow it too has not been decided, so ask before rewriting one.
- **Brand palette is white/black/red, plus one blue for actions** (see the next bullet). Red is an accent color only — logo, step numbers, card hover borders — never a background or text color for content. It lightens to `#ff4d57` in dark mode because `#e50914` drops below the contrast threshold on a dark ground.
- **Orange was tried and rejected** (2026-08-28, at the user's request then their call to drop it). Two things killed it: white text on any real orange lands at 2.8:1, so an orange button would have needed dark text and become the only dark-text button on the site; and orange can't be a text color on white at all, making it a fill-only half-color. It also sits ~28° from the brand red, close enough to read as "the red slipped" rather than a second color. Don't reintroduce it as a third accent without a job of its own.
- **Filled buttons are blue, not red** (changed 2026-08-28 at the user's request): a filled red rectangle reads as "delete / cancel / warning", the wrong signal for a confirming action. Primary buttons use the `--color-action` / `--color-action-dark` tokens (`bg-action` / `hover:bg-action-dark`) defined in `src/styles/global.css`; secondary buttons stay outlined neutral. Black was tried first and rejected as not eye-catching enough. The rule that settled out of this: **blue means clickable, red means brand.** Anything that invites a click — filled buttons, "read more" links, the colour a card title turns on hover — uses `action`; red is reserved for identity and decoration: the logo, the step numbers on the home page, the `hover:border-brand/40` card outline. The one exception is the white pill on a product panel, where blue would clash with the product's colour (see the product teasers bullet above).
- **Language coverage is asymmetric on purpose.** Turkish has published blog posts; English has none, so `/en/blog/` renders the `blog.empty` string. Both real products (Dernek Asistan and İbadet Rehberim) are published in both languages. The empty English blog is expected output, not a bug to chase.
- **Logo and favicon are the İ monogram** (black tile, white body with a diagonal cut, red dot centered above). Sources live in `brand/` (`logo.svg` plus PNG exports at 256/512/1024); the deployed favicon is `public/favicon.svg`, and `BaseLayout` also points the JSON-LD `logo` field at it. If the mark changes, regenerate all of them together.
- **Names inside `src/` are English, content is Turkish.** Asset folders and image files use English names (`_images/`, `screen-prayer-times.png`) even though the copy they carry is Turkish. The Turkish segments under `src/pages/` are the deliberate exception — those are the public URLs, not internal names. Project images live in `src/content/projects/_images/<proje-slug>/` and are referenced from frontmatter with paths relative to the markdown file, so Astro optimizes them (`image()` in the collection schema).
- **Each product image is generated with exactly one set of options**, from `productImageOptions(image)` in `src/utils/product.ts`: portrait phone screens get `PRODUCT_IMAGE_WIDTH` (640) at default quality; landscape desktop screenshots (Dernek Asistan) get `PRODUCT_WIDE_IMAGE_WIDTH` (1984) at `quality: 'max'`, because default WebP compression smears fine UI text (WebP is still ~40 KB at that setting). Asking for the same source with a second set of options makes Astro emit a second file that the browser also downloads, and `ProductPage` builds the JSON-LD `screenshot` URLs from the same call as the visible `<img>`, so every component must spread the helper rather than pass its own `width`/`quality`. Each width is 2× the widest place that shape is displayed, so 2× screens don't upscale it — which means **desktop screenshots must be captured at DPR 2** (Chrome DevTools device toolbar → DPR 2 → Capture screenshot; a 1× capture is built at its own width and stays soft on Retina; Astro never enlarges). `isLandscape()` also switches the layout: on the product page a phone screen sits beside the text in a capped column while a landscape shot drops below the text at full container width (hero and feature rows); on the product panel a phone screen stands as a phone and a landscape shot slides in from the edge. Change the constants, not the individual `<Image>` calls.
- **Store badge art is optional at build time.** `StoreBadges.astro` checks `public/badges/` on disk and falls back to its own text button when the official SVG/PNG is missing, so the repo builds without shipping Apple's and Google's assets. The brand constraints it encodes (App Store first, equal 48px heights, no recolouring or cropping, untranslated wordmarks, one file per language) come from those vendors' guidelines — `public/badges/README.md` has the download links and the rules.
- The sitemap integration (configured in `astro.config.mjs`) adds hreflang alternates only where the TR and EN paths are identical apart from `/en`, which means just the two home pages and the two blog indexes. Every pair with translated slugs (`/hakkimizda/` ↔ `/en/about/`, services, products) is linked only by the page-level `<link>` tags. SEO.md item 7 has the `serialize`-hook fix if the sitemap should match. `public/robots.txt` points to `/sitemap-index.xml`.
- Remaining pre-launch TODOs (og:image, contact form backend, hosting + DNS, Search Console, analytics) are tracked in README.md, which is written in Turkish and doubles as the content-authoring guide. Keep its checklist in sync when one lands; the matching `TODO:` comments sit in `BaseLayout.astro`. `SEO.md` (Turkish) is the SEO and performance audit of 2026-09-20. It lists what must keep working ("Doğru kurulmuş olanlar (bozmayın)") and ends with the code-side backlog ("Kod tarafına düşen iş listesi"). It is a dated snapshot, so re-check a finding against the code before acting on it, and tick its checklist when an item lands.
